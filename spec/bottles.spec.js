const expect = require('chai')
const bottleSong = require('../bottles.js')

describe('bottleSong', () => {
  describe('toFiftyOne', () => {
    it('Should be a string', () => {
      const num = 99
      const actual = 

    })
  })
  it()
})